import 'package:flutter/material.dart';

class NoteFoldersContent extends StatelessWidget {
  const NoteFoldersContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
